import React from "react";
import { CounterButtons } from "./counter-buttons";

export default function ReactCounterApp() {
  return <CounterButtons />;
}

// render(<ReactCounterApp />, document.getElementById("root"));
